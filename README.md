# Runes of Oblivion

> **Тактический roguelike deckbuilder с изометрической пошаговой боевой системой.**
>
> Построен с использованием **vanilla JavaScript** и **Vite** для максимальной производительности и модульной архитектуры.

---

## 📋 Оглавление

- [Описание проекта](#описание-проекта)
- [Основные возможности](#основные-возможности)
- [Технологический стек](#технологический-стек)
- [Начало работы](#начало-работы)
- [Структура проекта](#структура-проекта)
- [Архитектура](#архитектура)
- [Геймплей](#геймплей)
- [Разработка](#разработка)
- [Отладка](#отладка)
- [Развертывание](#развертывание)
- [Дополнительные ресурсы](#дополнительные-ресурсы)

---

## 🎮 Описание проекта

**Runes of Oblivion** — это roguelike deckbuilder, который сочетает стратегический выбор карт (deck building) с тактической пошаговой боевой системой на изометрическом виде. Игрокам предстоит составлять оптимальные комбинации карт, управлять позиционированием персонажей на поле боя и адаптироваться к неожиданным вызовам в каждом новом прохождении.

### 🎯 Вдохновение

Проект вдохновлен успешными играми жанра:

- **Slay the Spire** — систематика deck building и roguelike прогрессия
- **Into the Breach** — тактическое позиционирование на сетке и предсказуемость ходов
- **Hades** — roguelike структура и переиспользуемость контента между прогонами

---

## ✨ Основные возможности

### 🎲 Игровая механика

- **Система карт (Deck Building)** — составление и оптимизация колоды карт для боя
- **Пошаговая боевая система** — стратегический выбор действий каждый ход
- **Изометрическая тактика** — позиционирование на сетке с учетом расстояний и препятствий
- **Roguelike прогрессия** — каждое прохождение уникально с различными врагами и наградами
- **Система синергий** — комбинирование карт для усиления эффектов

### ⚡ Технические особенности

- **Быстрая загрузка** — благодаря Vite и ES модулям
- **Адаптивный дизайн** — оптимизирован для различных разрешений экрана
- **60 FPS** — гладкая отрисовка на Canvas
- **Сохранение состояния** — возможность паузы и продолжения игры

---

## 🛠 Технологический стек

| Компонент | Технология | Назначение |
|-----------|-----------|-----------|
| **Язык** | JavaScript (ES6+) | Основной язык разработки |
| **Build Tool** | Vite | Быстрая разработка и оптимизированные сборки |
| **Шаблонизация** | HTML5 | Структура приложения |
| **Стили** | CSS3 | Визуальное оформление UI |
| **Графика** | HTML5 Canvas | Отрисовка игровых объектов и эффектов |
| **Состояние** | Vanilla JS (Event-driven) | Управление состоянием без зависимостей |
| **Версионирование** | Git | Контроль версий кода |

### 🚀 Почему vanilla JavaScript?

- **Минимальные зависимости** → быстрая загрузка и отсутствие нежелательных включений
- **Полный контроль над архитектурой** → гибкость в проектировании
- **Идеально для обучения** → понимание всех деталей реализации
- **Лучшая оптимизация** → прямая работа с браузерными API без промежуточных слоев

---

## 🚀 Начало работы

### 📋 Предварительные требования

```bash
Node.js >= 20.19 или >= 22.12
npm >= 9.0
git
```

### 📥 Установка

```bash
# Клонировать репозиторий
git clone https://github.com/Mihaaaail/Runes_of_oblivion.git
cd Runes_of_oblivion

# Установить зависимости
npm install

# Запустить dev сервер
npm run dev
```

Откройте браузер на `http://localhost:5173` (порт может отличаться — смотрите вывод в терминал).

### 🔨 Production сборка

```bash
# Создать оптимизированную сборку
npm run build

# Проверить production версию локально
npm run preview
```

Готовые файлы будут в папке `dist/`.

---

## 📁 Структура проекта

```
Runes_of_oblivion/
├── public/                  # Статические ассеты
│   ├── sprites/            # Графические ассеты персонажей и врагов
│   └── assets/             # Иконки, текстуры, звуки
│
├── src/                     # 🎯 ИСХОДНЫЙ КОД
│   ├── GameManager.js       # Главный контроллер приложения
│   ├── main.js              # Entry point
│   │
│   ├── core/                # ⚙️ ЯДРО ИГРОВОЙ ЛОГИКИ
│   │   ├── logic/           # Алгоритмы и вычисления (чистые функции)
│   │   │   ├── AILogic.js         # 🤖 Поведение врагов и AI
│   │   │   ├── BattleLogic.js     # ⚔️ Система боя, урон, эффекты
│   │   │   ├── CardEffects.js     # 🎴 Реализация эффектов карт
│   │   │   └── PathFinding.js     # 🗺️ A* и навигация на сетке
│   │   │
│   │   └── state/           # 📊 Управление состоянием
│   │       ├── CardModel.js       # 📋 Модель карты
│   │       ├── GameState.js       # 🎮 Глобальное состояние игры
│   │       ├── GridModel.js       # 📐 Модель игровой сетки
│   │       └── UnitModel.js       # 👤 Модель персонажа/врага
│   │
│   ├── data/                # 📦 ДАННЫЕ И КОНФИГУРАЦИЯ
│   │   ├── CardLibrary.js         # 🎴 Библиотека всех карт
│   │   └── constants.js           # ⚙️ Глобальные константы
│   │
│   ├── run/                 # 🎲 ROGUELIKE СИСТЕМА
│   │   ├── RunController.js       # 🎮 Управление игровым сеансом
│   │   ├── RunFlowController.js   # 🔄 Цикл боя, переходы
│   │   ├── RunMapGenerator.js     # 🌍 Генерация карт уровней
│   │   └── RunMapView.js          # 🗺️ Навигация по карте
│   │
│   ├── ui/                  # 🖼️ ИНТЕРФЕЙС
│   │   └── UIManager.js           # Отрисовка UI элементов
│   │
│   ├── utils/               # 🔧 УТИЛИТЫ
│   │   ├── EventEmitter.js        # 📢 Система событий
│   │   └── IsoMath.js             # 📐 Математика изометрии
│   │
│   └── view/                # 🎨 ОТРИСОВКА (CANVAS)
│       ├── GameRenderer.js        # 🖌️ Главный рендерер
│       ├── GridRenderer.js        # 📊 Отрисовка сетки
│       └── UnitRenderer.js        # 👥 Отрисовка персонажей
│
├── index.html               # HTML шаблон
├── vite.config.js           # Конфигурация Vite
├── package.json             # Зависимости и npm скрипты
├── package-lock.json        # Lockfile зависимостей
├── .gitignore               # Git исключения
└── README.md                # Документация (этот файл)
```

### 📝 Описание ключевых модулей

#### **GameManager.js**
Главный контроллер приложения. Координирует всё поведение игры и управляет переходами между различными состояниями (меню → боевая сцена → результаты).

#### **core/logic/** — Алгоритмы (чистые функции)
- `AILogic.js` — выбор действий врагами на основе состояния игры
- `BattleLogic.js` — расчёт урона, применение эффектов, проверка победы
- `CardEffects.js` — как каждая карта влияет на состояние игры
- `PathFinding.js` — поиск пути A* для врагов и юнитов

#### **core/state/** — Управление состоянием
- `GameState.js` — центральное хранилище текущего состояния
- `GridModel.js` — представление игровой сетки (клетки, препятствия)
- `UnitModel.js` — игрок, враги, их позиции, HP, статусы
- `CardModel.js` — карты в руке, колода, эффекты

#### **data/** — Конфигурация
- `CardLibrary.js` — определения всех карт (урон, стоимость, эффекты)
- `constants.js` — боевые параметры, размеры сетки и т.д.

#### **run/** — Roguelike система
- `RunController.js` — управление текущим прохождением (progress, достижения)
- `RunFlowController.js` — цикл: выбор уровня → боевая сцена → награда → новый уровень
- `RunMapGenerator.js` — процедурная генерация уровней
- `RunMapView.js` — UI для выбора и навигации по доступным уровням

#### **view/** — Отрисовка на Canvas
- `GameRenderer.js` — главный рендерер, управляет всеми рендерерами
- `GridRenderer.js` — отрисовка сетки, клеток, эффектов на земле
- `UnitRenderer.js` — отрисовка спрайтов персонажей, анимации, здоровья

#### **utils/IsoMath.js**
Математические функции для изометрической проекции:
- Конвертация между экранными координатами и координатами сетки
- Расстояния между клетками
- Направления и углы

---

## 🏗 Архитектура

### 📚 Слоистая архитектура (Layered Architecture)

```
┌───────────────────────────────────────────┐
│         UIManager (UI слой)               │
│    Отрисовка интерфейса, карты в руке    │
├───────────────────────────────────────────┤
│  GameManager + RunController (App слой)   │
│    Управление потоком игры, переходы      │
├───────────────────────────────────────────┤
│  BattleLogic + AILogic (Business слой)    │
│    Алгоритмы боя, AI, эффекты карт       │
├───────────────────────────────────────────┤
│  GameState + Models (Data слой)           │
│    Хранилище состояния, модели данных     │
├───────────────────────────────────────────┤
│  GameRenderer (View слой)                 │
│    Отрисовка на Canvas, анимации          │
└───────────────────────────────────────────┘
```

Каждый слой взаимодействует через **EventEmitter**, избегая прямых зависимостей между модулями.

### 🎯 Принципы дизайна

#### 1. **Event-driven система**

Модули не вызывают друг друга напрямую, а отправляют события:

```javascript
// Отправка события
EventEmitter.emit('cardPlayed', { card, targetPos });

// Прослушивание события
EventEmitter.on('cardPlayed', (data) => {
  applyCardEffect(data.card, data.targetPos);
});
```

**Преимущества:**
- ✅ Модули полностью независимы
- ✅ Легко добавлять новые функции
- ✅ Простое тестирование
- ✅ Избегаем циклических зависимостей

#### 2. **Разделение ответственности (Separation of Concerns)**

```
core/logic/       → Чистые функции, никакие побочные эффекты
core/state/       → Управление состоянием
view/             → Только отрисовка на Canvas
run/              → Бизнес-логика roguelike системы
utils/            → Вспомогательные функции
```

#### 3. **Canvas-based отрисовка**

- Используется HTML5 Canvas для всей графики
- `requestAnimationFrame` для 60 FPS
- Изометрическая проекция через `IsoMath`

---

## 🎮 Геймплей

### 🔄 Основной цикл игры

```
1️⃣  ФАЗА ПОДГОТОВКИ
    ├─ Начисление маны
    ├─ Раздача карт из колоды
    └─ Вывод доступных действий

2️⃣  ФАЗА ХОДОВ ИГРОКА
    ├─ Выбор карты из руки
    ├─ Выбор цели/позиции на сетке
    ├─ Выполнение действия карты
    └─ Повтор, пока хватает маны

3️⃣  ФАЗА ХОДОВ ВРАГОВ
    ├─ AI выбирает действия для каждого врага
    ├─ Враги совершают ходы
    └─ Применяются эффекты урона

4️⃣  ПРОВЕРКА УСЛОВИЙ
    ├─ Враги еще живы? → Вернуться к шагу 1
    ├─ Враги побеждены? → Награда за уровень
    └─ Игрок повергнут? → Game Over
```

### 🎴 Система карт

Каждая карта имеет следующую структуру:

```javascript
{
  id: 'strike',                    // Уникальный ID
  name: 'Удар',                    // Название
  cost: 1,                         // Стоимость в мане
  damage: 8,                       // Наносимый урон
  range: 1,                        // Дальность (в клетках)
  description: 'Нанести урон соседнему врагу',
  effects: [                       // Специальные эффекты
    { type: 'stun', duration: 1 }
  ],
  rarity: 'common'                 // Редкость
  // common, uncommon, rare, legendary
}
```

### 🤖 Враги и сложность

- **AI враги** имеют собственную логику для выбора действий
- **Сложность** увеличивается с каждым уровнем
- **Roguelike элементы** гарантируют разнообразие в каждом прохождении

---

## 👨‍💻 Разработка

### 🔧 Запуск в режиме разработки

```bash
npm run dev
```

**Включает:**
- ✅ Hot Module Replacement (HMR) — автоматическая перезагрузка при сохранении
- ✅ Source maps — легкая отладка
- ✅ Chrome DevTools — встроенные инструменты браузера
- ✅ Быстрые пересборки

### 🔨 Сборка проекта

```bash
npm run build
```

Создает оптимизированную версию в папке `dist/`:
- ✅ Минифицированный JavaScript и CSS
- ✅ Tree-shaking неиспользуемого кода
- ✅ Оптимизация изображений и ассетов
- ✅ Готово к продакшену

### 🛠 Рекомендуемые инструменты

| Инструмент | Назначение |
|-----------|-----------|
| **VS Code** | Основной редактор с расширениями для JS |
| **Chrome DevTools** | Отладка, профилирование, сетевой анализ |
| **Performance tab** | Анализ FPS и загрузки памяти |

### 📐 Конвенции кода

```javascript
// ✅ 1. Используйте const и let, не var
const gameState = { /* ... */ };
let counter = 0;

// ✅ 2. Функции в logic/ должны быть чистыми (pure functions)
function calculateDamage(baseDamage, playerStats) {
  return baseDamage * (1 + playerStats.strength / 100);
}

// ✅ 3. Документируйте сложные функции
/**
 * Вычисляет следующий ход врага с учетом позиции игрока
 * @param {Position} currentPos - текущая позиция врага
 * @param {Unit} player - объект игрока (цель)
 * @returns {Position} позиция для следующего хода
 */
function calculateNextMove(currentPos, player) { /* ... */ }

// ✅ 4. Для доступа к состоянию используйте GameState
import { gameState } from './core/state/GameState.js';

function getDamage() {
  return gameState.player.stats.damage;
}

// ✅ 5. Отправляйте события через EventEmitter
import { EventEmitter } from './utils/EventEmitter.js';

EventEmitter.emit('cardPlayed', { card, targetUnit });

// ✅ 6. Используйте классы для сложных объектов
class Card {
  constructor(id, name, cost) {
    this.id = id;
    this.name = name;
    this.cost = cost;
  }

  getDescription() {
    return `${this.name} (стоимость: ${this.cost})`;
  }
}
```

### 🎯 Разработка по слоям

При добавлении новой функции следуйте этому порядку:

#### 1️⃣ **Logic слой** — Чистые функции

```javascript
// core/logic/BattleLogic.js
export function calculateTurnOrder(units) {
  return units.sort((a, b) => b.speed - a.speed);
}
```

#### 2️⃣ **State слой** — Интеграция в состояние

```javascript
// core/state/GameState.js
import { calculateTurnOrder } from '../logic/BattleLogic.js';

export class GameState {
  updateTurnOrder() {
    this.turnOrder = calculateTurnOrder(this.allUnits);
    // Отправить событие
    EventEmitter.emit('turnOrderCalculated', { order: this.turnOrder });
  }
}
```

#### 3️⃣ **View слой** — Отрисовка результатов

```javascript
// view/GameRenderer.js
EventEmitter.on('turnOrderCalculated', (data) => {
  updateTurnIndicator(data.order);
  render();
});
```

### 🎴 Добавление новой карты

**Шаг 1:** Определите карту в `data/CardLibrary.js`

```javascript
export const CardLibrary = {
  // ... существующие карты ...
  
  strike: {
    id: 'strike',
    name: 'Удар',
    cost: 1,
    damage: 8,
    range: 1,
    description: 'Нанести урон соседнему врагу'
  },
  
  // ← Добавьте здесь новую карту
  fireball: {
    id: 'fireball',
    name: 'Огненный шар',
    cost: 3,
    damage: 15,
    range: 3,
    areaOfEffect: 2,
    description: 'Нанести урон в радиусе 2 клетки'
  }
};
```

**Шаг 2:** Реализуйте эффект в `core/logic/CardEffects.js`

```javascript
export function applyCardEffect(card, caster, target, battleState) {
  if (card.id === 'strike') {
    const damage = card.damage + (caster.stats?.strength || 0);
    target.takeDamage(damage);
    EventEmitter.emit('damageDealt', { 
      attacker: caster, 
      target, 
      amount: damage 
    });
  }
  
  if (card.id === 'fireball') {
    // Урон в радиусе
    const affectedUnits = getUnitsInRadius(target.position, card.areaOfEffect);
    affectedUnits.forEach(unit => {
      unit.takeDamage(card.damage);
    });
    EventEmitter.emit('areaEffectApplied', { position: target.position });
  }
}
```

**Шаг 3:** Интегрируйте в боевую систему `core/logic/BattleLogic.js`

```javascript
function executePlayerAction(card, targetUnit) {
  applyCardEffect(card, player, targetUnit, gameState);
  
  // Отправить событие
  EventEmitter.emit('cardExecuted', { card, target: targetUnit });
  
  // Перейти к ходу врагов
  await executeEnemyTurns();
}
```

**Шаг 4:** Добавьте анимацию в `view/UnitRenderer.js` (если нужна)

```javascript
EventEmitter.on('cardExecuted', (data) => {
  if (data.card.id === 'fireball') {
    playExplosionAnimation(data.target.position);
  }
});
```

---

## 🐛 Отладка

### 🔍 Включение отладочного режима

```javascript
// data/constants.js
export const DEBUG = true;
export const SHOW_GRID = true;
export const SHOW_HITBOXES = true;
export const SHOW_PATHS = true;
```

**Это позволит:**
- ✅ Выводить координаты сетки на поле боя
- ✅ Показывать hitbox'ы врагов и юнитов
- ✅ Выводить пути поиска (PathFinding)
- ✅ Логировать события в консоль

### ⚠️ Частые ошибки и решения

| Проблема | Решение |
|----------|---------|
| 🤖 Враги не движутся | Проверьте `core/logic/AILogic.js` и возвращаемое значение `calculateNextMove()` |
| 🎴 Карты не срабатывают | Проверьте `core/logic/CardEffects.js` и отправляемые события в `EventEmitter` |
| 🎨 Карты не отрисовываются | Проверьте `view/UnitRenderer.js` и Canvas context инициализацию |
| 👥 Враги проходят сквозь друг друга | Проверьте коллизии в `core/state/GridModel.js` |
| ⚡ Производительность упала | Используйте Chrome DevTools Performance tab для профилирования |
| 🔊 События не срабатывают | Убедитесь, что EventEmitter.on() вызывается ДО EventEmitter.emit() |

### 🖥️ Инструменты для отладки

```javascript
// В консоли браузера (F12):

// 1. Проверить состояние игры
console.log(gameState);

// 2. Отследить все события
EventEmitter.on('*', (event, data) => {
  console.log(`📢 Event: ${event}`, data);
});

// 3. Проверить позиции юнитов
console.log(gameState.allUnits.map(u => ({
  name: u.name,
  position: u.position,
  hp: u.hp,
  status: u.status
})));

// 4. Проверить состояние сетки
console.log(gameState.grid.getCellsInfo());

// 5. Проверить карты в руке
console.log(gameState.player.hand.map(c => ({
  name: c.name,
  cost: c.cost,
  damage: c.damage
})));
```

---

## 📦 Развертывание

### 🌐 GitHub Pages

```bash
# 1. Создать production сборку
npm run build

# 2. Содержимое папки dist/ автоматически деплоится на GitHub Pages
# (если настроено в Settings → Pages)
```

### 🚀 Другие платформы

**Поддерживаемые способы развертывания:**

| Платформа | Команда | Примечания |
|-----------|---------|-----------|
| **Vercel** | `npm run build` → upload `dist/` | Автоматический CI/CD из GitHub |
| **Netlify** | Подключить repo → автосборка | Самый простой способ |
| **GitHub Pages** | `npm run build` → push в gh-pages | Встроенная поддержка |
| **Любой static hosting** | Загрузить содержимое `dist/` | Работает везде |

---

## 🤝 Вклад в проект

Если вы хотите улучшить проект:

1. **Создайте feature branch:**
   ```bash
   git checkout -b feature/amazing-feature
   ```

2. **Внесите изменения и commit:**
   ```bash
   git commit -m 'Add amazing feature'
   ```

3. **Push в репозиторий:**
   ```bash
   git push origin feature/amazing-feature
   ```

4. **Откройте Pull Request на GitHub**

**Рекомендации:**
- ✅ Следуйте конвенциям кода (см. раздел "Конвенции кода")
- ✅ Добавляйте комментарии к сложным алгоритмам
- ✅ Тестируйте перед push'ем
- ✅ Обновляйте документацию при добавлении функций

---

## 📚 Дополнительные ресурсы

### 📖 Обучающие материалы

**Vite и сборка:**
- [Vite Documentation](https://vitejs.dev/) — официальная документация
- [Vite Guide](https://vitejs.dev/guide/) — начало работы

**Canvas и отрисовка:**
- [HTML5 Canvas API](https://developer.mozilla.org/en-US/docs/Web/API/Canvas_API) — MDN
- [Canvas Tutorial](https://developer.mozilla.org/en-US/docs/Web/API/Canvas_API/Tutorial) — пошаговый гайд

**JavaScript:**
- [JavaScript ES6+ Features](https://es6.io/) — современный JavaScript
- [MDN Web Docs](https://developer.mozilla.org/) — справочная информация

### 🎮 Game Design References

- [Into the Breach Design](https://www.youtube.com/watch?v=rDKj8I6vLJY) — анализ тактического дизайна
- [Slay the Spire Design](https://www.youtube.com/watch?v=9HO20yRNJ4E) — структура roguelike
- [Game Loop Design](https://www.davidepesce.com/articles-html/game-loop/) — паттерны игровых циклов

### ⚡ Оптимизация и производительность

- [Canvas Performance Tips](https://developer.mozilla.org/en-US/docs/Web/API/Canvas_API/Tutorial/Optimizing_canvas) — оптимизация Canvas
- [JavaScript Performance](https://developer.mozilla.org/en-US/docs/Web/Performance) — инструменты профилирования
- [requestAnimationFrame](https://developer.mozilla.org/en-US/docs/Web/API/window/requestAnimationFrame) — анимационный цикл

---

## 📄 Лицензия

MIT

---

## 👨‍🔧 Контакты и ссылки

- **GitHub (автор):** [@Mihaaaail](https://github.com/Mihaaaail)
- **Repository:** [Runes_of_oblivion](https://github.com/Mihaaaail/Runes_of_oblivion)
- **Issues:** [Report bugs and request features](https://github.com/Mihaaaail/Runes_of_oblivion/issues)

---


**Версия документации:** 2.0  
**Последнее обновление:** Декабрь 2025

⭐ Если проект тебе нравится, не забудь поставить звёздочку! ⭐
